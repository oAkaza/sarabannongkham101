<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>✏️ แก้ไขประกาศ</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Thai&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Noto Sans Thai', sans-serif; }
    label { font-weight: bold; }
  </style>
</head>
<body class="bg-gradient-to-br from-red-100 via-white to-pink-100 py-10">
  <div class="max-w-3xl mx-auto bg-white shadow-xl rounded-xl p-8 border border-red-200">
    <h1 class="text-3xl font-bold text-center text-red-800 mb-6">✏️ แก้ไขประกาศ</h1>
    <form method="POST" action="/announce/edit/<%= index %>" enctype="multipart/form-data" class="space-y-4">
      <div>
  <label for="announceNo">เลขที่ประกาศ</label>
  <input type="text" name="announceNo" id="announceNo" value="<%= announce.announceNo %>" required class="w-full px-4 py-2 border rounded" />
</div>

<div>
  <label for="subject">เรื่อง</label>
  <input type="text" name="subject" id="subject" value="<%= announce.subject %>" class="w-full px-4 py-2 border rounded" />
</div>

<div>
  <label for="announceDate">วันที่ประกาศ</label>
  <input type="date" name="announceDate" id="announceDate" value="<%= announce.announceDate %>" class="w-full px-4 py-2 border rounded" />
</div>

<div>
  <label for="action">การปฏิบัติ</label>
  <select name="action" id="action" class="w-full px-4 py-2 border rounded">
    <% const actions = ['สำนักปลัด','กองคลัง','กองช่าง','กองสวัสดิการสังคม','กองสาธารณสุข','หน่วยตรวจสอบภายใน','นิติกร','ป้องกัน','ฝ่ายนโยบายและแผน','ฝ่ายทรัพยากรบุคคล','ฝ่ายบริหารงานทั่วไป']; %>
    <% actions.forEach(act => { %>
      <option value="<%= act %>" <%= announce.action === act ? 'selected' : '' %>><%= act %></option>
    <% }) %>
  </select>
</div>


      <div>
        <label for="pdfFile">แนบไฟล์ PDF ใหม่ (ถ้ามี)</label>
        <input type="file" name="pdfFile" accept=".pdf" class="w-full" />
        <% if (announce.pdfFile) { %>
          <p class="text-sm mt-1">📎 ไฟล์เดิม: <a href="/uploads/<%= announce.pdfFile %>" target="_blank" class="text-blue-600 underline">ดูไฟล์</a></p>
        <% } %>
      </div>

      <div class="flex justify-between pt-4">
        <a href="/announce/list" class="px-4 py-2 bg-gray-500 hover:bg-gray-600 text-white rounded shadow">ย้อนกลับ</a>
        <button type="submit" class="px-6 py-2 bg-red-600 hover:bg-red-700 text-white rounded shadow">💾 บันทึกการแก้ไข</button>
      </div>
    </form>
  </div>
</body>
</html>
