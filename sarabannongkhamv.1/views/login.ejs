<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>เข้าสู่ระบบ</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Thai&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Noto Sans Thai', sans-serif; }
    input:focus { outline: none; border-color: #2563eb; box-shadow: 0 0 0 1px #2563eb; }
  </style>
</head>
<body class="bg-gradient-to-b from-blue-200 to-white flex justify-center items-center min-h-screen">
  <div class="bg-white p-8 rounded-2xl shadow-2xl w-full max-w-md border border-blue-100">
    <h2 class="text-2xl font-bold text-center text-blue-800 mb-6">📘 ระบบสารบรรณ อบต.หนองขาม</h2>

    <% if (error) { %>
      <div class="text-red-600 text-sm mb-4 text-center bg-red-50 border border-red-200 rounded py-2 px-4">
        <%= error %>
      </div>
    <% } %>

    <form method="POST" action="/login" class="space-y-4">
      <input type="text" name="username" placeholder="👤 ชื่อผู้ใช้" required
             class="w-full p-2 border border-gray-300 rounded-lg focus:ring focus:ring-blue-100" />
      <input type="password" name="password" placeholder="🔒 รหัสผ่าน" required
             class="w-full p-2 border border-gray-300 rounded-lg focus:ring focus:ring-blue-100" />
      <button type="submit"
              class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg font-semibold transition duration-200">
        🚪 เข้าสู่ระบบ
      </button>
    </form>
  </div>
</body>
</html>
